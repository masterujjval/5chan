#!/bin/bash

sudo cp client.py /usr/local/bin
sudo chmod +x /usr/local/bin/client.py
sudo mv /usr/local/bin/client.py /usr/local/bin/5chan
echo "5chan client installed successfully. You can run it using the command '5chan'... Be an anon!"
